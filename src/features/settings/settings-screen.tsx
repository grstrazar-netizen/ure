import { Card } from "@/components/ui/card";

export function SettingsScreen() {
  return (
    <Card className="p-4">
      <h2 className="text-lg font-semibold">Settings</h2>
      <p className="mt-1 text-sm text-muted">Profile, plan limits, reminders, future Slovenia tax options.</p>
    </Card>
  );
}
